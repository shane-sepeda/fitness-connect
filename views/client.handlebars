<div class="container-fluid">
    
      <div class="row">
        <div class="col-sm-3 col-md-2 sidebar">
          <ul class="nav nav-sidebar">
            <li class="active"><a href="#"><span class="sr-only">(current)</span></a></li>
            <li><a href="#">Schedule</a></li>
            <li><a href="#">Clients</a></li>
            <li><a href="#">Reports</a></li>
            <li><a href="#">Trainers</a></li>
            <li><a href="#">Locations</a></li>
          </ul>
        </div>
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          

          <div class="row placeholders">
            <div class="col-xs-6 col-sm-3 placeholder">
              <!-- PlaceHolder -->
            <div class="client-data"><h2>{{dbClientData.first_name}} {{dbClientData.last_name}}</h2>
            <p>{{dbClientData.birth_date}}</p>
            <p>{{dbClientData.address}}</p>
            <p>{{dbClientData.phone_no}}</p>
            <p>{{dbClientData.interest}}</p>
            <p>{{dbClientData.trainer_id}}</p>
            </div>
             </div>
          </div>
          <div class="table-responsive">
            <!-- Placeholder -->
          </div>
        </div>
      </div>

      <h2 class="sub-header"> </h2>
      <div class="table-responsive">
        <!-- Placeholder -->
      </div>
    </div>
